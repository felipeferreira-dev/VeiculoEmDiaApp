<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.dev.felipeferreira.veiculoemdia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VeículoEmDia">

        <activity
            android:name=".controller.MenuNavActivity"
            android:label="@string/title_activity_menu_nav"
            android:launchMode="singleTask"
            android:theme="@style/Theme.VeículoEmDia.NoActionBar" />


        <activity
            android:name=".controller.MainActivity"
            android:launchMode="standard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>